export enum Mode {
  Compare,
  Chart,
  UniquesChart,
}
export enum UiStateMsg {
  SetModeCompare,
  SetModeChart,
  SetModeUniquesChart,
  SetCurrentIdx0,
  SetCurrentIdx1,
  SetCurrentIdx2,
  ToggleExpand,
}
