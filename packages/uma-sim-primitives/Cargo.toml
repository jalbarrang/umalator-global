[package]
name = "uma-sim-primitives"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true
publish.workspace = true
description = "Pure shared primitives for the Uma Musume race simulation: cross-context value objects (RNG, IDs, regions, math, ubiquitous-language enums) and course leaf formulas (coefficients, phase, geometry)."

[dependencies]
serde = { version = "1", features = ["derive"] }
rand_xoshiro = "0.7"
rand_core = "0.9"

[dev-dependencies]
serde_json = "1"

[lints]
workspace = true
