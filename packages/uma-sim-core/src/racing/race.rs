//! The `Race` **aggregate root** — owns the `Runner` entities, enforces
//! invariants (gate assignment, finish ordering, pacer selection) and drives
//! the per-tick simulation loop.

// Populated by t-017.
