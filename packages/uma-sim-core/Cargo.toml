[package]
name = "uma-sim-core"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true
publish.workspace = true
description = "Deterministic Uma Musume race simulation domain (DDD core library)."

[dependencies]
serde = { version = "1", features = ["derive"] }
rand_xoshiro = "0.7"
rand_core = "0.9"

[dev-dependencies]
criterion = "0.5"
serde_json = "1"

[[bench]]
name = "race_sim"
harness = false

[lints]
workspace = true
