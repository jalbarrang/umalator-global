name: React Doctor

on:
  pull_request:
  push:
    branches: [main]

permissions:
  contents: read

jobs:
  react-doctor:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6.0.2
        with:
          fetch-depth: 0 # required for `diff`

      - name: Setup Bun
        uses: oven-sh/setup-bun@v2

      - name: Install dependencies
        run: bun install --frozen-lockfile

      - name: Run React Doctor
        run: |
          if [ "${{ github.event_name }}" = "pull_request" ]; then
            bun run ql:react -- . --yes --verbose --annotations --fail-on warning --diff origin/${{ github.base_ref }}
          else
            bun run ql:react -- . --yes --verbose --annotations --fail-on warning --diff ${{ github.event.before }}
          fi
